# Tarefas

> Versão em português de `TASKS.md`, renomeado nesta fase pela Regra Permanente nº 1 (português como idioma oficial — ver `RELATORIO_FASE_4_5.md`).

Checklist granular da fase em andamento, mais um backlog do que vem a seguir. Ver `ROADMAP.md` para a lista completa de fases e a razão de cada uma, e `/dev` + `/dev/ceo` (não-produção) para o mesmo status como um dashboard.

## Fase 0 — Processo e visibilidade ✅

- [x] `ROADMAP.md`, `PROXIMAS_TAREFAS.md`, `dev-status.json`, `src/app/dev/page.tsx`

## Fase 1 — Campaign Resolution Engine ✅

- [x] Modelos `Campaign`/`CampaignAssignment`/`RedirectLog`, resolvedor desacoplado e cacheável
- [x] `/r/[code]` resolve via o motor, 100% retrocompatível para empresas sem campanhas
- [x] Corrigido um gap real encontrado via teste: Redis configurado-mas-inalcançável derrubava rotas públicas (cache + rate-limit)
- [x] `RELATORIO_FASE_1.md` entregue

## Fase 2 — Campaign Manager V2 ✅

- [x] Schema: modelos `Branch`, `Zone` (mínimos, escopados por empresa); `NFCCard.branchId`/`zoneId`
- [x] Schema: `Campaign` ganha `status` (substitui `active`), `description`, `recurrenceType`/`recurrenceConfig`, `tags`, `ownerId`
- [x] Schema: `CampaignAssignment` + `TargetScope` expandidos para `COMPANY`/`BRANCH`/`ZONE`/`CARD`
- [x] Camada de domínio (`src/domain/campaign/`): `status.ts` (derivação de status de exibição), `destination.ts` (metadados de tipo/agrupamento de renderização), `assignment.ts` (especificidade de escopo + invariantes de formato) — puro, sem I/O
- [x] Camada de repositório (`src/repositories/`): `campaign.repository.ts`, `branch.repository.ts`, `zone.repository.ts` — Prisma bruto, sem regras de negócio
- [x] Camada de serviço: `campaign.service.ts` (list/get/create/update/duplicate/archive/delete/assign/unassign), `branch.service.ts`, `zone.service.ts`
- [x] Corrigida proativamente uma race condition real: requisições concorrentes de atribuição em escopo COMPANY, fechada com uma transação Serializable (`CampaignConflictError` / P2034)
- [x] Corrigido proativamente um gap real entre tenants: atualizações de `branchId`/`zoneId` de cartão agora validam que o alvo pertence à mesma empresa
- [x] Renderizador de destino isomórfico compartilhado (`src/lib/campaign-destination.ts`) + construtor de UTM (`src/lib/utm.ts`) — o redirecionamento do motor e o preview do Builder nunca podem discordar silenciosamente
- [x] Extraído um `slugify()` compartilhado (`src/lib/slugify.ts`) após capturar um bug real: slugs de UTM não removiam acentos ("Aniversário" → "anivers-rio")
- [x] `resolveDestination`/`resolve.ts`/`data.ts` atualizados para escopo em 4 níveis e `status` em vez de `active`; `/r/[code]` simplificado para usar o renderizador de destino compartilhado
- [x] Rotas de API: `/api/campaigns/**` (CRUD, duplicar, arquivar, atribuições), `/api/branches`, `/api/zones`
- [x] UI do dashboard: lista de campanhas (busca/filtro/ordenação), sheet do Campaign Builder (seletor visual de destino, preview ao vivo, UI de recorrência, tags, dono), Assignment Manager (4 escopos + criação rápida inline de zona/branch)
- [x] Diálogo de edição de cartão ganhou atribuição de branch/zona (sem isso, campanhas em escopo ZONE/BRANCH não teriam nenhum cartão elegível alcançável pela UI)
- [x] Visão executiva `/dev/ceo` (antecipada da Fase 12 a pedido); `/dev` e `/dev/ceo` agora compartilham `src/lib/dev-status.ts`
- [x] Seed: demo Bella Vista (3 zonas, 5 cartões, 4 campanhas cobrindo todos os escopos/status)
- [x] Teste de fumaça interativo via um harness temporário de dados mockados (apagado antes da entrega) — capturou o bug de acento no UTM ao vivo, não só via tipos
- [x] Verificado: `tsc`, `eslint`, `prisma validate`, `npm run build` todos limpos
- [x] `RELATORIO_FASE_2.md` entregue

## Fase 3 — Motor de Regras + testes A/B ✅

- [x] Schema: `Company.timezone` (padrão `America/Sao_Paulo`); enum `RuleType`; modelo `Rule` (combinado com AND por campanha); modelo `CampaignVariant` (A/B ponderado); modelo `RuleExecutionLog`; `RedirectLog.variantId`
- [x] Camada de domínio (`src/domain/rules/`): `timezone.ts` (decomposição de data/hora local baseada na `Intl` nativa, sem biblioteca de datas), `evaluate.ts` (avaliação por tipo de regra, falha fechada em config malformada), `recurrence.ts` (traduz `recurrenceType`/`recurrenceConfig` da Fase 2 para o mesmo caminho de avaliação de regras — um motor, não dois), `variants.ts` (seleção aleatória ponderada, RNG injetável para testes)
- [x] Deliberadamente NÃO adicionadas dimensões de regra de mesa/zona/unidade/estado-de-campanha — já cobertas por `CampaignAssignment.scope` e a ordenação existente de prioridade/janela; verificado diretamente que "campanha encerrada → volta ao padrão" já funciona via a ordenação existente, nenhuma regra nova necessária para isso
- [x] Integração com o motor: `resolve.ts` avalia regras (próprias + derivadas de recorrência) por candidato, mantém a ordenação inalterada de especificidade→prioridade→recência, seleciona uma variante A/B quando presente; `index.ts` propaga `deviceType` (analisado uma vez em `/r/[code]/page.tsx`, compartilhado entre `generateMetadata` e a página para o `React.cache()` deduplicar) e grava linhas de `RuleExecutionLog` só para candidatos que de fato tinham regras
- [x] Design de cache inalterado desde a Fase 1/2 — regras/variantes viajam junto no blob de cache de campanhas por empresa já existente; tipo de dispositivo nunca toca a camada de cache, é puramente uma entrada de runtime para a função de decisão
- [x] Corrigido proativamente um bug real: mudar o `type` de destino de uma campanha enquanto ela tem variantes A/B quebrava-as silenciosamente (configs antigas de variante não batiam mais com o schema do novo tipo) — agora bloqueado com um erro claro em vez de um placeholder silencioso de "Em breve" aparecendo depois
- [x] Lógica de avaliação verificada diretamente com casos de teste construídos à mão antes de confiar nela (não só via tipos): janela sexta 18-22h, fronteira de dia da semana, virada de janela de horário durante a madrugada (22:00-02:00), filtro de dispositivo, comportamento de falha fechada em config malformada, e convergência de variante ponderada em 100 mil simulações (config 70/30 → 70,1%/29,9% real)
- [x] Rotas de API: `/api/campaigns/[id]/rules`, `/api/campaigns/[id]/rules/[ruleId]`, `/api/campaigns/[id]/variants`, `/api/campaigns/[id]/variants/[variantId]`
- [x] UI do dashboard: duas novas abas no Builder — Regras (seletor de tipo + campos condicionais por tipo de regra) e A/B (nome/peso da variante + reaproveita `DestinationConfigFields` para o tipo da própria campanha)
- [x] Código morto removido de passagem: `countCompanyScopeAssignments` em `campaign.repository.ts` estava definido mas nunca chamado (a Fase 2 corretamente colocou uma verificação equivalente inline dentro da própria transação)
- [x] Seed: o Happy Hour da Bella Vista agora é de fato aplicado (não só capturado), uma regra `DEVICE_TYPE` no Google Reviews VIP, um teste A/B 70/30 na Black Friday
- [x] Teste de fumaça interativo via harness temporário de dados mockados (apagado antes da entrega) — confirmou que as duas novas abas e seus campos condicionais por tipo renderizam e alternam corretamente
- [x] Verificado: `tsc`, `eslint`, `prisma validate`, `npm run build` todos limpos
- [x] `RELATORIO_FASE_3.md` entregue

## Fase 4 — Multi-unidade + RBAC v2 ✅

- [x] `DECISOES_DE_ARQUITETURA.md` criado antes de qualquer código da Fase 4, por instrução explícita — ADR-001 a ADR-012 escritas retroativamente para as Fases 1-3
- [x] `ROADMAP.md` ganhou a seção permanente "Architect Review" — sequência obrigatória de 8 passos antes de qualquer fase a partir da Fase 4 ser considerada concluída
- [x] Schema: modelo `Organization` (wrapper opcional acima de `Company`); `Company.organizationId` (opcional, aditivo); enum `Role` substituído (Owner/Admin/Marketing/Manager/Operator/Read-Only, `STAFF`→`OPERATOR` como padrão); `TargetScope` ganha `ORGANIZATION`; modelo `UserAccessScope` (restrição opt-in de branch/zona); modelos `AuditLog`/`AuditAction`; `CampaignAssignment.organizationId` (o único campo que cruza fronteiras de empresa de propósito)
- [x] Camada de domínio: `domain/rbac/roles.ts` (matriz de permissões, não hierarquia de papéis), `domain/rbac/scope.ts` (verificação de restrição como lista de permissão), `domain/audit/labels.ts`; `domain/campaign/assignment.ts` estendido para escopo `ORGANIZATION`
- [x] `lib/auth.ts`: `AuthContext` ganha `organizationId`/`accessScopes`, dobrado na consulta de autenticação única já existente (sem N+1); `requirePermission()`/`requireScopeAccess()` adicionados ao lado do legado `requireRole()`
- [x] Serviços: `organization.service.ts` (create/get/rename), `audit.service.ts` (`recordAudit`, só na camada de fronteira — ver ADR-017), `team.service.ts` ganha concessão/listagem/revogação de escopo de acesso; `assignCampaign`/`unassignCampaign` de `campaign.service.ts` estendidos para escopo `ORGANIZATION` + verificação de escopo de acesso
- [x] Rotas de API: `/api/organization`, `/api/team/[id]/access-scopes`, `/api/team/[id]/access-scopes/[scopeId]`; toda rota pré-existente de campanha/equipe/branch/zona/empresa varrida de `requireRole(["OWNER","ADMIN"])` para `requirePermission(ctx, "...")`
- [x] Motor de resolução: `loadOrganizationCampaigns()` (a única consulta entre empresas, filtrada por `organizationId`, não `companyId`), sua própria camada de cache (`resolutionCacheKeys.organizationCampaigns`), buscada em `index.ts` só quando `company.organizationId` não é nulo — zero custo extra para empresas sem uma
- [x] UI do dashboard: `AssignmentManager` ganha escopo `ORGANIZATION` (ícone, label de alvo, construção do corpo) + trava `canManageStructure` na criação rápida inline de branch/zona (separada de `campaign:assign`); `TeamView` migrado para o novo conjunto de papéis + UI `AccessScopeManager` de restrição por membro; `OrganizationCard` (criar/ver uma organização) e `AuditLogCard` (últimas 50 ações) adicionados a Configurações
- [x] Achados do Architect Review, encontrados e corrigidos proativamente antes deste relatório:
  - Toda rota de API pré-existente de campanha/equipe/branch/zona/empresa ainda usava `requireRole(ctx, ["OWNER","ADMIN"])` enquanto a UI do dashboard já travava pelas novas permissões `campaign:write`/`team:write` — um usuário Marketing/Manager veria um controle de "gerenciar" que retornava 403 ao ser usado. Corrigido em 12 arquivos de rota.
  - `assignCampaign` não tinha verificação de escopo de acesso para escopo `CARD`, e nenhuma verificação impedindo um usuário restrito por branch/zona de criar uma atribuição em escopo `COMPANY` ou `ORGANIZATION` (ambos alcançam mais do que qualquer restrição conseguiria expressar). Corrigido; `unassignCampaign` estendido com a verificação simétrica para que um usuário restrito não consiga remover o que não poderia ter criado.
  - O escopo `ORGANIZATION` tinha o mesmo gap de colunas NULL do escopo `COMPANY` (ADR-007 da Fase 2): o padrão "NULLs são distintos" do Postgres faz a constraint `@@unique` também não impedir linhas duplicadas de escopo `ORGANIZATION`. Fechado com a mesma proteção de transação Serializable, estendida para cobrir ambos os escopos.
  - Procurado e não encontrado nenhum N+1 introduzido por esta fase: a expansão do auth context (organizationId + accessScopes) viaja na consulta única já existente; a busca de cache de campanhas de organização é condicional e totalmente pulada no caso comum (sem organização).
- [x] Seed: `Organization` "Rede Bella Vista" envolvendo a empresa Bella Vista existente; segunda empresa "Bella Vista Shopping" (sua própria `Branch`/`Zone`/cartão) demonstrando alcance de franquia; uma campanha "Programa de Fidelidade" em escopo `ORGANIZATION` resolvendo nos cartões de ambas as empresas; um `MANAGER` restrito à zona VIP via `UserAccessScope`
- [x] Teste de fumaça interativo via harness temporário de dados mockados (apagado antes da entrega, middleware temporariamente desabilitado para o teste já que não existe instância Clerk ao vivo neste sandbox, restaurado logo depois) — confirmou que o escopo `ORGANIZATION` renderiza corretamente no Select e no label de alvo, que `canManageStructure` esconde/mostra corretamente o controle de criação rápida, e a renderização de `OrganizationCard` (vazio/preenchido) e `AuditLogCard`
- [x] Verificado: `tsc`, `eslint`, `prisma validate`, `npm run build` todos limpos
- [x] `RELATORIO_FASE_4.md` entregue

## Fase 5 — Mapa de Mesas ✅

- [x] Schema: enum `TableShape` (`RECTANGLE`/`CIRCLE`); `NFCCard` ganha `layoutX`/`layoutY` (opcionais — "não posicionado" até ser arrastado para o canvas pela primeira vez), `layoutWidth`/`layoutHeight`/`layoutRotation`/`tableShape`/`seats` (com padrão) — ver ADR-019 para o porquê disso viver direto em `NFCCard`, não em uma nova entidade
- [x] Camada de domínio: `domain/table-map/geometry.ts` (snap em grade, interseção de retângulo de seleção, conversão tela↔mundo — puro, sem I/O), `domain/table-map/status.ts` (`computeAllTableStatuses` — reaproveita `scopeSpecificityRank` da camada de domínio da Fase 1 em vez de reimplementar o motor, ver ADR-020); `DESTINATION_META` ganha uma `color` por tipo de campanha para o tingimento de status/Ghost Mode
- [x] Serviços: `table-map.service.ts` (`listCardsForMap`, `listActiveCampaignsForMap`, `listAssignmentsForStatus`); `card.service.ts` ganha `updateCardLayout`/`bulkUpdateCardLayout`/`duplicateCard`; `campaign.service.ts` ganha `bulkAssignCampaignToCards` para o fluxo de arrastar-e-soltar em lote com múltipla seleção
- [x] Rotas de API: `PATCH /api/cards/[id]/layout`, `PATCH /api/cards/layout` (em lote, uma transação para arrastar várias mesas), `POST /api/cards/[id]/duplicate`, `POST /api/campaigns/[id]/assignments/bulk`
- [x] UI do dashboard (`/dashboard/table-map`, novo item de menu "Mapa de Mesas"): seletor de abas de zona (`Todas`/cada zona/`Sem zona`) construído como alvos de drop de HTML5 nativo (soltar = escopo `ZONE` ou `COMPANY`); canvas com pan/zoom (roda do mouse para pan, Ctrl/Cmd+roda ou botões para zoom) com grade de fundo e alternância de snap em grade; arraste de mesas baseado em pointer events (uma só ou toda a multi-seleção junto, com throttle via rAF para que um mouse rápido não dispare um commit do React por evento nativo) com `React.memo` + culling de viewport para que mesas fora da tela nunca renderizem; seleção múltipla por retângulo (marquee); renomeação inline por duplo clique; duplicar selecionadas; uma bandeja de "mesas não posicionadas" com um fluxo de armar-então-clicar-para-posicionar (mais simples e robusto que calcular coordenadas exatas de soltura por um segundo canal de drag-and-drop nativo sem posição existente de onde arrastar)
- [x] Arrastar e soltar campanha: bandeja lateral de campanhas ativas como chips `draggable` nativos; soltar sobre uma mesa única, um grupo com múltipla seleção, ou uma aba de zona/empresa todos chamam o endpoint certo; **Ghost Mode** — toda mesa afetada tinge-se ao vivo com a cor do tipo da campanha arrastada (`DESTINATION_META.color`) com uma transição suave em CSS *antes* da soltura, incluindo previews de toda a seleção e de toda a zona, exatamente o efeito pedido de "mesas da varanda ficam azuis... VIP douradas... internas verdes"
- [x] Indicadores de status e conflito: cada mesa tinge-se com a cor da campanha atualmente vencedora; uma mesa com duas ou mais atribuições ativas empatadas em especificidade e prioridade ganha um selo de conflito (tooltip mostra a contagem de concorrentes)
- [x] Adição "uau" do Product Review (encontrada proativamente, não pedida item a item): um breve pulso de escala e brilho confirma uma soltura bem-sucedida na(s) mesa(s) afetada(s), reaproveitando o mesmo sistema de cores do preview do Ghost Mode para que toda a interação se leia como uma única peça contínua de feedback, em vez de um toast estático isolado
- [x] Achados do Architect Review, encontrados e corrigidos proativamente antes deste relatório:
  - Um usuário só-Marketing (`campaign:assign` mas não `card:write`) não conseguia selecionar nenhuma mesa, porque a seleção de mesa estava travada atrás da permissão de edição — bloqueando exatamente o fluxo de atribuição em lote para o qual aquela permissão existe. Corrigido: a seleção agora é alcançável por qualquer uma das duas permissões; só o arraste de movimento em si (e sua persistência) continua atrás de `card:write`, então uma tentativa de arraste acidental de um não-editor não arrasta mais visualmente uma mesa e depois volta ao lugar sem explicação.
  - Reexaminar as garantias de unicidade de `CampaignAssignment` ao construir o endpoint de atribuição em lote revelou que as verificações de existência só-COMPANY/ORGANIZATION da Fase 2 (ADR-007) e da Fase 4 deixavam os escopos `CARD`/`ZONE`/`BRANCH` expostos ao mesmo gap de colunas NULL do Postgres — um bug latente desde a Fase 2, não algo que a Fase 5 introduziu, mas que o próprio código desta fase teria herdado e obscurecido ainda mais. Generalizada a verificação para todo escopo de forma uniforme. Ver ADR-021.
  - Verificado que nenhum N+1 foi introduzido: o carregamento inicial do Mapa de Mesas é 5 consultas paralelas (cartões, zonas, branches, campanhas ativas, atribuições ativas), nenhuma aninhada; o cálculo de status é uma única passada em memória, não uma consulta por mesa.
- [x] Seed: as 3 zonas da Bella Vista agora têm ~50 mesas posicionadas no total (VIP/Varanda/Interno, ~16-18 cada, formas retangular/circular misturadas), mais os 5 cartões originais também posicionados (nada fica "não posicionado" por padrão); duas novas campanhas em escopo de zona ("Varanda no Instagram", "Cardápio do Salão Interno") ao lado do Happy Hour VIP já existente, então cada zona visivelmente tinge de uma cor diferente logo de início
- [x] Teste de fumaça interativo via harness temporário de dados mockados (apagado antes da entrega, middleware temporariamente desabilitado para o teste, restaurado logo depois — sem Clerk/Postgres ao vivo neste sandbox): confirmada a coloração de status por zona e por cartão contra as cores reais de `DESTINATION_META`; a transição de cor ao vivo do Ghost Mode (verificada via disparo direto de `DragEvent`, já que a ferramenta de automação de mouse deste sandbox não sintetiza gestos nativos de drag HTML5 — confirmadas atualizações reais de estilo inline, não só o texto do overlay); o endpoint correto disparado na soltura para alvos únicos/em lote/zona/empresa; seleção múltipla por retângulo; arraste único e em grupo com snap em grade persistindo corretamente; o fluxo de armar-então-posicionar de mesa não posicionada; a lógica subjacente de renomeação (confirmada via disparo direto de `dblclick` — o próprio gesto de duplo clique da ferramenta de navegador não registrava de forma confiável como um duplo clique nativo sob a captura de ponteiro deste componente, uma limitação da ferramenta de teste, não um bug do componente)
- [x] Verificado: `tsc`, `eslint`, `prisma validate`, `npm run build` todos limpos
- [x] `RELATORIO_FASE_5.md` entregue, incluindo a nova seção de Product Review

## Fase 4.5 — NFC OS Design Language & Integração de MCPs ✅

- [x] **Regra Permanente nº 1 (português)** aplicada antes de qualquer trabalho de design: `ARCHITECTURE_DECISIONS.md`→`DECISOES_DE_ARQUITETURA.md`, `TASKS.md`→`PROXIMAS_TAREFAS.md` (ambos traduzidos por completo), `ROADMAP.md` traduzido no lugar, `PHASE_N_REPORT.md`→`RELATORIO_FASE_N.md` (renomeados; conteúdo histórico mantido em inglês com nota explicativa — decisão deliberada, ver `RELATORIO_FASE_4_5.md`), `dev-status.json`/`/dev`/`/dev/ceo` traduzidos, referências cruzadas em código atualizadas
- [x] **Detecção de MCPs**: nenhum conector (Shadcn UI MCP, Magic UI MCP, Design MCP, Shadcn Space) estava instalado ou disponível no registro deste ambiente — verificado via busca real, não presumido. Instruções de instalação manual verificadas e documentadas em `ROADMAP.md` ("Integração de MCPs"). Todo componente desta fase foi construído à mão, sobre a stack já existente
- [x] Arquitetura: `packages/{ui,design-tokens,icons,animations}` como pastas com alias de import (`@nfc-os/*`) no `tsconfig.json`, não um monorepo real — ver ADR-022
- [x] `packages/design-tokens`: durações/easings/springs de motion, escala de z-index, breakpoints (fonte da verdade para cor/radius/sombra continua em `globals.css`, via `@theme` do Tailwind v4)
- [x] `globals.css`: cor de marca própria (violeta-índigo `oklch(0.549 0.214 279)`, claro e escuro — ver ADR-023) substituindo o cinza padrão do shadcn/ui em `--primary`/`--ring`/`--sidebar-primary`/`--accent`; tokens de sombra (`--shadow-subtle/elevated/premium/glow`), easing (`--ease-standard/enter/exit/spring`) e keyframes (`fade-in`, `scale-in`, `shimmer`) como namespaces nativos do Tailwind v4; utilitários `.glass`, `.skeleton-shimmer`, `.hover-lift`; `prefers-reduced-motion` respeitado globalmente, não componente a componente
- [x] `packages/icons`: `NfcOsIcon` — encapsulamento de conceitos de domínio (Campanha, Zona, Organização, Ghost Mode, etc.) sobre `lucide-react`, para que uma futura troca de biblioteca de ícones toque um arquivo, não cada tela
- [x] `packages/animations`: variants do Framer Motion (`fadeIn`, `scaleIn`, `slideUp`, `slideInFromRight`, `staggerContainer`, `successPop`, `hoverLift`, `pressScale`) e `usePrefersReducedMotion()`
- [x] `cmdk` adicionado como dependência (única biblioteca nova desta fase) para o Command Palette — acessibilidade e navegação por teclado corretas sem reimplementar à mão
- [x] 12 componentes premium em `packages/ui`: Card KPI, Card Analytics, Card Campanha, Card Zona, Card Mesa, Card Organização (`OrganizationBrandCard`), Modal/Drawer/Popover Premium, Command Palette, Empty States, Skeleton States, Timeline, Activity Feed, Heatmap Card, Badge Inteligente, Avatar Stack, Permission Matrix — todos com dark mode automático via os mesmos tokens CSS
- [x] Aplicados de verdade (não só construídos) em: Dashboard (Visão Geral) — `StatCard`/`AnalyticsCard`; Configurações — `PermissionMatrix` (nova seção "Papéis e permissões"), `AuditLogCard` reescrito sobre `ActivityFeed`, `OrganizationCard` reescrito sobre `OrganizationBrandCard`; Campanhas — `StatusBadge` reescrito sobre `SmartBadge`, `EmptyState` na lista vazia; `/dev` e `/dev/ceo` — reconstruídos por completo como "painel de investimento" (KPIs, Saúde do Projeto com quality gates, Arquitetura, Timeline animada, Backlog priorizado, contagem de ADRs computada ao vivo do arquivo, não hardcoded)
- [x] `DashboardCommandPalette` (Cmd+K/Ctrl+K) integrado ao layout do dashboard inteiro, com navegação para todas as seções
- [x] Glassmorphism aplicado com moderação — só no header fixo do dashboard, não em superfícies de leitura
- [x] `MANIFESTO_DO_DESIGN.md` criado — 8 princípios (uma ação principal por tela, toda animação comunica estado, nenhum modal sem motivo, no máximo 3 níveis de profundidade visual, menos cliques sempre vence, consistência antes de originalidade por tela, todo estado vazio é uma oportunidade, cor tem significado)
- [x] Architect Review + Product Review rigoroso, achados registrados em `RELATORIO_FASE_4_5.md`, incluindo um bug real de runtime encontrado e corrigido (icones como referência de componente quebrando a serialização do React Server Components — ver ADR-024) e a admissão honesta de que a aplicação visual NÃO cobre 100% das telas existentes (Mapa de Mesas, Cartões, Equipe, lista de Campanhas mantêm sua UI já testada, só com os tokens de cor/sombra globais herdados automaticamente — não uma reescrita de componente por componente)
- [x] Verificado: `tsc`, `eslint`, `prisma validate`, `npm run build` todos limpos; verificação interativa real via dev server local (não apenas harness mockado, já que `/dev`/`/dev/ceo` não dependem de Clerk/Postgres) confirmou o bug de serialização e sua correção
- [x] `RELATORIO_FASE_4_5.md` entregue, incluindo Architect Review e Product Review rigoroso

## Fase 6 — Live Mode + Heatmap ✅

- [x] **Live Mode:** `src/app/api/live/stream/route.ts` — SSE (`ReadableStream`, `runtime="nodejs"`) com polling de `RedirectLog`/`RatingEvent`/`PrivateFeedback`/`AuditLog` a cada 2s (`services/live.service.ts`'s `listRecentEvents`), heartbeat a cada 15s, auto-fechamento em 50s com evento `reconnect` carregando o último `since`. Ver ADR-025 para por que SSE, não WebSocket real, nesta fase
- [x] `src/lib/live/use-live-connection.ts` — hook cliente: `EventSource` com reconexão exponencial (1s→30s), watchdog de heartbeat (30s), máquina de estado `connecting/connected/reconnecting/offline`, cada aba com sua própria conexão (sincronização = convergência de estado, não uma aba líder)
- [x] `ConnectionIndicator` em `packages/ui` — badge de status com ponto pulsante só quando `connected`
- [x] **Heatmap Inteligente:** `domain/heatmap/` (tipos, `normalizeCounts`, `intensityFromRecency` — decaimento em degraus, não curva contínua) + `services/heatmap.service.ts` (uma consulta por camada, reaproveitando exatamente as tabelas que já existem); rota `GET /api/heatmap`; 6 camadas (Aproximações, Conversões, Google Reviews, Instagram, Campanha atual, Última interação) — "Campanha atual" reaproveita o `statusMap` da Fase 5 em vez de recalcular (ver ADR-025)
- [x] `HeatmapLayerToggle` no Mapa de Mesas — seletor de camada; `TableNode` ganha coloração por intensidade (`color-mix` com `--brand`, mesma escala do `HeatmapCard` genérico da Fase 4.5) quando uma camada está ativa, substituindo a coloração por status sem duplicar renderização
- [x] **Live Presence:** eventos ao vivo com `cardId` não-nulo disparam o mesmo `celebrate()` já existente da Fase 5 (pulso de escala/brilho); eventos `ASSIGNMENT_CHANGED` (sem `cardId`, escopo de zona/empresa) disparam um refetch de `/api/table-map/assignments` em vez de tentar pulsar uma mesa específica
- [x] **Ghost Mode Evolution:** `GhostModePreviewDialog` — toda mudança em massa (grupo de mesas, zona, empresa) mostra contagem, nomes das mesas afetadas e uma estimativa de impacto real (toques das últimas 24h nessas mesas exatas, via `/api/heatmap`) antes de aplicar; mesa única continua instantânea. `bulkAssignCampaignToCards` migrado para `createManyAndReturn`, devolvendo os IDs criados; novo endpoint `DELETE /api/campaigns/[id]/assignments/bulk` para desfazer exatamente esse lote; toast de sucesso de toda atribuição (mesa única ou em massa) ganhou um botão "Desfazer". Ver ADR-028
- [x] **Time Machine:** `services/time-machine.service.ts` (`getSnapshotAt`, `getWindowSummary`) reconstrói, por mesa, a última campanha realmente servida até um instante passado a partir de `RedirectLog` — deliberadamente não finge reconstruir um histórico de atribuições que o schema não tem (ver ADR-026, incluindo o limite documentado de 3000 linhas por consulta). `TimeMachinePanel` — slider de 0-24h atrás, resumo (toques e mesas que mais converteram na janela)
- [x] **Playback Mode (opcional):** `GET /api/live/history` reaproveita `listRecentEvents` numa janela maior (até 24h, limite de 300 eventos) em ordem cronológica; `usePlayback` reproduz em intervalos de 350ms, disparando o mesmo `onEvent` do Live Mode real — mesas acendem exatamente como aconteceu, sem caminho de renderização separado
- [x] **Command Center:** `/dev/ceo/command-center` — compõe `TableMapView` inteiro (Live Mode/Heatmap/Time Machine já embutidos, somente leitura), KPIs animados (toques hoje, avaliações hoje, mesas com atividade agora, mesas com campanha ativa), `LiveEventFeed`, lista de campanhas ativas. Precisou de um pequeno par de rotas somente-leitura em `/api/dev/demo/*`, escopadas fixas à empresa "Bella Vista" (ver ADR-027) — a única exceção arquitetural desta fase, e deliberadamente mínima
- [x] `KpiCard` (packages/ui) ganhou uma transição de "pop" ao trocar de valor — encontrado proativamente durante o Product/Demo First Review: um KPI que muda sozinho e não comunica isso visualmente não parece "ao vivo." Beneficia tanto o Command Center quanto o `/dev/ceo` executivo existente
- [x] Seed da Bella Vista: atividade sintética (toques/avaliações/feedbacks privados das últimas 36h, com peso maior nas horas recentes) para Heatmap/Feed de Eventos/Time Machine/Command Center não abrirem vazios — nenhuma dessas 4 tabelas tinha uma linha sequer até esta fase
- [x] Achados do Architect Review, corrigidos proativamente: `bulkAssignCampaignToCards` não devolvia os IDs criados (bloqueador direto do Desfazer do Ghost Mode Evolution) — migrado para `createManyAndReturn`; Time Machine e Heatmap são mutuamente exclusivos na UI (dois significados de cor não podem coexistir na mesma mesa ao mesmo tempo)
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos
- [x] Verificação interativa via harness temporário (`src/app/dev/harness-phase6`, apagado antes da entrega) com `middleware.ts`/`ClerkProvider` temporariamente bypassados (sem Clerk real neste sandbox) — confirmou Live Mode, as 6 camadas de Heatmap (cores computadas conferidas via `getComputedStyle`, não só rótulos), Time Machine, Playback, Ghost Mode Evolution (diálogo + estimativa de impacto genuína + POST real) e o Command Center (KPIs animados, feed de eventos, modo somente leitura) — ver `RELATORIO_FASE_6.md`
- [x] `RELATORIO_FASE_6.md` entregue, incluindo Architect Review, Product Review e a nova **Demo First Review**

## Fase 7 — Analytics Enterprise ✅

- [x] **Regra Permanente — Revenue Review** instituída como quarto processo obrigatório (ao lado de Architect/Product/Demo First), com 5 perguntas em todo relatório de fase a partir de agora — ver `ROADMAP.md`
- [x] **Schema:** `Company` ganhou `roiAvgTicket`/`roiReturnRate`/`roiNewCustomerValue` (nulos até configurados — ROI Mode nunca estima com dado parcial); `Campaign` ganhou `estimatedCost` opcional, para "ROI da campanha"/"custo por avaliação" — ver ADR-029
- [x] **Domínio:** `domain/analytics/{types,math,funnel,insights,forecast,roi}.ts` — todas as regras de negócio puras e testáveis sem banco, incluindo o motor de regras determinístico dos Insights Automáticos
- [x] **5 motores desacoplados** (`analytics-engine`, `insights-engine`, `forecast-engine`, `ranking-engine`, `export-engine` `.service.ts`) — ver ADR-030 para a arquitetura e o limite estrutural real descoberto (campanha nunca gera avaliação neste modelo de dados — toda comparação de campanha usa toques, zona/mesa/funcionário usam conversões de verdade)
- [x] **KPIs executivos:** aproximações hoje, conversões, avaliações geradas, CTR do fluxo de avaliação, taxa de conversão, crescimento semanal/mensal, receita estimada (ROI Mode) — mais 6 cartões "melhor X" (campanha/zona/mesa/funcionário/horário/dia)
- [x] **Funil Inteligente** (Aproximação → Página aberta → Clique → Conversão → Avaliação publicada, escopado ao fluxo de avaliação), com animação de largura suave por etapa (`FunnelChart`, packages/ui)
- [x] **Insights Automáticos:** comparação de conversão por zona, uso de campanha vs. média, pico de engajamento por horário de um cartão, e um alerta de queda dia-a-dia — cada um com limiar de amostra/significância e evidência numérica anexada
- [x] **Ranking Inteligente:** campanha/zona/mesa/funcionário/horário/dia, com filtro de período e fuso horário local (reaproveita `getLocalDateParts` do Rule Engine) — "funcionário" continua sendo só um `NFCCard` tagueado "equipe" (ADR-019), sem entidade nova
- [x] **Comparativos automáticos:** hoje vs. ontem, semana vs. semana anterior, mês vs. mês anterior
- [x] **Timeline Executiva:** atribuições de campanha (`AuditLog`), início/fim de campanha agendada, pico de aproximações e recorde de avaliações — cada entrada com instante específico linka para o Time Machine da Fase 6
- [x] **Forecast Inteligente:** meta de avaliações configurável com projeção linear a partir do ritmo recente, e tendência de toques da campanha mais usada — sempre rotulado como estimativa
- [x] **ROI Mode:** painel em Configurações (`RoiSettingsForm`) + painel no Analytics Enterprise mostrando receita estimada com a fórmula exposta, ou uma chamada para configurar quando ainda não preenchido
- [x] **Exportações Enterprise:** CSV achatado (Seção/Métrica/Valor), Excel com `exceljs` (5 abas formatadas), PDF executivo com `@react-pdf/renderer` (sem headless browser — ver ADR-031)
- [x] **`/dashboard/analytics`** ganhou uma aba "Enterprise" (nova aba padrão) com seletor de período (7/30/90 dias) e menu de exportação, mantendo a aba "Visão geral" (Fase 0) e "Feedbacks privados" inalteradas
- [x] **Command Center Evolution:** `/dev/ceo/command-center` ganhou funil vivo, insights/alertas inteligentes, ranking líder por categoria, timeline executiva e forecast, com refresh de 30s via as novas rotas `/api/dev/demo/analytics/*` (mesmo padrão da ADR-027)
- [x] **`packages/ui`** ganhou `InsightCardList`, `RankingList` e `FunnelChart` — todos com animações suaves (Framer Motion) e reaproveitados tanto no dashboard real quanto no Command Center
- [x] **Seed da Bella Vista:** segundo cartão de equipe ("Garçom João"), janela de 14 dias de histórico (antes 36h — dá massa real para comparativos semana/mês e forecast), e vieses documentados e deliberados para a história "Happy Hour no horário certo, Varanda em destaque, Mesa VIP e João líderes" emergir de números reais recalculados
- [x] Achados do Architect Review, corrigidos proativamente: um insight de campanha inicialmente desenhado como "gerou N avaliações extras" foi corrigido para "foi acionada N vezes a mais" antes de qualquer código depender do formato errado, ao se descobrir que campanhas nunca passam pelo fluxo de estrelas; a montagem de URL do Live Mode/Heatmap/Time Machine (herdada da Fase 6) foi corrigida de `${apiBase}/api/...` para `${apiBase}/...` com `apiBase` passando a incluir o prefixo completo — um 404 silencioso que só aparecia com um `apiBase` customizado
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo o empacotamento de `exceljs`/`@react-pdf/renderer` no build de produção
- [x] Verificação interativa via harness temporário (`src/app/dev/harness-phase7`, apagado antes da entrega) com `middleware.ts`/`ClerkProvider` temporariamente bypassados — confirmou KPIs/Funil/Insights/Comparativos/Timeline/ROI Mode, troca de aba de ranking com dado real e cores de medalha computadas, seletor de período disparando as 6 chamadas corretas, resiliência a falha (dados anteriores mantidos), e os 3 formatos de exportação gerando arquivos reais e válidos (CSV/XLSX com assinatura ZIP/PDF com assinatura %PDF-) via uma rota mock que chama as funções de renderização de verdade — ver `RELATORIO_FASE_7.md` para o escopo não coberto (Command Center Evolution verificado por tipos + reuso de componente, não remontado com um segundo dataset)
- [x] `RELATORIO_FASE_7.md` entregue, incluindo Architect Review, Product Review, Demo First Review e a nova **Revenue Review**

## Fase 8 — Event sourcing + filas + observabilidade ✅

- [x] **Regra Permanente — Reliability Review** instituída como quinto processo obrigatório (ao lado de Architect/Product/Demo First/Revenue), com 6 perguntas em todo relatório de fase a partir de agora — ver `ROADMAP.md`
- [x] **Schema:** modelo `EventLog` novo (sem foreign key para `Company`/`Organization` — ver ADR-032); `Company` ganhou `webhookUrl`/`webhookSecret` opcionais
- [x] **Event Bus** (`lib/event-bus/`): `domain/events/types.ts` com os 10 eventos de domínio tipados de ponta a ponta; `publishEvent` (grava em `EventLog`, depois roteia best-effort para filas, nunca lança); `EVENT_SUBSCRIPTIONS` mapeando cada evento às filas inscritas; `replayExistingEvent` para o Event Replay reenfileirar sem duplicar a trilha de auditoria
- [x] **10 eventos publicados nos pontos de mutação reais**, mirando o padrão já existente de `recordAudit` (chamado da camada de rota, não do serviço): campanhas (criar/atualizar/arquivar/excluir), zonas (criar/excluir), cartões (criar/atualizar/excluir), organização (criar/renomear) — todos na camada de rota; `NFCTocado`/`RedirecionamentoResolvido` dentro do próprio `resolution-engine` (fluxo público sem `ctx`); `AvaliacaoPublicada`/`FeedbackRecebido` dentro de `rating.service.ts`/`feedback.service.ts` (idem), ambos via `after()` não-bloqueante
- [x] **Observability Engine:** `lib/observability/correlation.ts` (Correlation ID/Request ID via `AsyncLocalStorage`), `logger.ts` (logs estruturados, escopados aos módulos novos desta fase), `tracing.ts` (OpenTelemetry real, `ConsoleSpanExporter` — nenhum coletor configurado, ver ADR-034), `cache-metrics.ts`, `sse-metrics.ts` (contador real de conexões ativas em `/api/live/stream`), `trace-metrics.ts` (spans recentes, não "traces ativos" — ver ADR-034); `src/instrumentation.ts` inicializa tracing + Sentry via o hook oficial do Next.js
- [x] **Sentry preparado:** `sentry.server.config.ts`/`sentry.edge.config.ts`/`instrumentation-client.ts`, `onRequestError` em `instrumentation.ts` — todos no-op sem `NEXT_PUBLIC_SENTRY_DSN`, nenhum projeto Sentry real configurado neste ambiente (ver ADR-034)
- [x] **Queue Engine** (`lib/queues/`): `connection.ts` (conexão `ioredis` separada via `REDIS_URL`, `null` com degradação graciosa sem ela), `definitions.ts` (6 filas com retry exponencial + dead-letter via listener de `QueueEvents`), `idempotency.ts` (`SET NX EX` via o Redis REST já existente)
- [x] **Worker Engine** (`lib/workers/`): `processors.ts` (um processador por fila, idempotência + correlação + span), `heartbeat.ts`, `drain.ts` (drenagem por tempo limitado, ~45s), `run-workers.ts` (entrypoint sempre-ativo para deploy não-serverless futuro, com ressalva documentada sobre `tsx` e alias `@/`); `/api/queues/process` (protegida por `CRON_SECRET`, `runtime="nodejs"`, `maxDuration=60`); `vercel.json` com o Cron a cada minuto — ver ADR-033
- [x] **Circuit Breaker** (`lib/circuit-breaker.ts`) protegendo o worker de webhooks (HMAC-SHA256, timeout de 10s); workers de WhatsApp/e-mail são stubs honestos (nenhuma integração real existe)
- [x] **Chaos Engine** (`lib/chaos/flags.ts`): 5 flags, bloqueadas em produção por duas camadas independentes; integrado em `resolution-engine/cache.ts` (custo zero em produção — gate de `NODE_ENV` antes de qualquer leitura de Redis) e nos processadores de fila; `/api/dev/chaos` (GET/POST) — ver ADR-035
- [x] **Event Replay** (`services/replay.service.ts`): `reconstructSequence` (somente leitura) e `replayToQueues` (reenfileira preservando o `id` original); `/api/dev/replay` (GET/POST)
- [x] **Segurança operacional:** rate limit por organização (`queueRouting`, 500/min) no roteamento de eventos para filas — nunca afeta o redirecionamento nem a gravação em `EventLog`
- [x] **Painel de Saúde** (`/dev/ceo/reliability`): `services/reliability.service.ts` agrega Redis (REST+TCP)/filas/workers/cache/eventos-por-minuto/conexões SSE/spans recentes/Chaos Mode em uma única leitura, tudo com degradação graciosa própria; UI com polling de 5s, toggles de Chaos Mode, painel de Event Replay, botão de Simulador de Carga
- [x] **Mission Control** (`/dev/ceo/mission-control`): tela única estilo NOC combinando `TableMapView` ao vivo (somente leitura) com o mesmo `reliability.service` do Painel de Saúde
- [x] **Simulador de Carga da Bella Vista** (`/api/dev/load-simulator`): dispara uma rajada de eventos reais via `publishEvent` contra a empresa de demonstração
- [x] `next.config.ts` ganhou `serverExternalPackages: ["bullmq", "ioredis"]` — corrige um aviso real de build (BullMQ tentando empacotar backends opcionais nunca instalados, ex. `@valkey/valkey-glide`)
- [x] Achado e corrigido antes da entrega: `/api/dev/load-simulator` não tinha `try/catch` na rota (só nos itens do laço interno) — uma falha do Postgres antes do laço (ex.: empresa de demonstração inalcançável) retornava um 500 com corpo vazio em vez de um JSON de erro tratável; corrigido para usar `handleApiError`, mesmo padrão de toda outra rota do produto. Mesma correção aplicada a `/api/dev/replay`
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo o empacotamento de `bullmq`/`ioredis`/`@opentelemetry/*`/`@sentry/nextjs` no build de produção
- [x] Verificação interativa via harness temporário (`middleware.ts`/`ClerkProvider` bypassados, restaurados depois) — confirmou o Painel de Saúde degradando graciosamente com Redis/Postgres reais ausentes (o cenário mais importante de provar numa fase de confiabilidade), o toggle de Chaos Mode end-to-end (POST real, estado refletido na UI e persistido), e o tratamento de erro do Event Replay/Simulador de Carga contra um Postgres genuinamente inalcançável neste sandbox — ver `RELATORIO_FASE_8.md` para o que ficou fora do alcance (Mission Control/Command Center exigem a empresa de demonstração num banco alcançável, uma limitação já aceita desde a Fase 6, não uma regressão desta fase)
- [x] `RELATORIO_FASE_8.md` entregue, incluindo Architect Review, Product Review, Demo First Review, Revenue Review e a nova **Reliability Review**

## Fase 9 — API pública v1 + SDK + webhooks ✅

- [x] **Regras Permanentes — Platform First Review, WOW Factor Review, Zero Dívida Silenciosa** instituídas (a primeira como sexto processo obrigatório, ao lado de Architect/Product/Demo First/Revenue/Reliability; as outras duas como regras permanentes de engenharia) — ver `ROADMAP.md`
- [x] **Schema:** `Company.webhookUrl`/`webhookSecret` (Fase 8) removidos; modelos novos `ApiKey`, `WebhookEndpoint`, `WebhookDelivery` (com `@@unique([endpointId, eventId])`), `ApiRequestLog` — ver ADR-036/037
- [x] **`src/app/api/v1/`** isolado das rotas internas, nunca misturado — ver ADR-036
- [x] **Plumbing compartilhado** (`lib/api-v1/`): `handler.ts` (`withApiV1` — auth, rate limit, idempotência, log de requisição, envelope de erro, tudo num único wrapper), `auth.ts` (`requireApiKey`, `generateApiKey`/hash SHA-256, `buildSyntheticAuthContext` para reaproveitar `assignCampaign`/RBAC), `pagination.ts` (cursor, `{ data, has_more, next_cursor }`), `idempotency.ts` (`Idempotency-Key` com replay de resposta e proteção contra corrida concorrente via placeholder `PENDING`), `errors.ts` (`{ error: { code, message, request_id } }`, `notFoundIfMissing` traduz `ForbiddenError` de busca-por-id para 404)
- [x] **Escopos de API** (`domain/api-v1/scopes.ts`): 15 escopos granulares (`cards:read/write`, `campaigns:read/write`, `zones:read/write`, `branches:read/write`, `organizations:read/write`, `analytics:read`, `events:read`, `feedback:read/write`, `webhooks:manage`)
- [x] **10 recursos públicos implementados**, todos reaproveitando os serviços internos já existentes (nunca duplicando regra de negócio): cards, campaigns (+ assignments), zones, branches, organizations (recurso singular), analytics (kpis/funnel/rankings), events (somente os 8 com nome público), feedback, webhooks (+ deliveries + replay)
- [x] **Nova permissão RBAC `developers:manage`** (`domain/rbac/roles.ts`, só OWNER/ADMIN) — API key/webhook alcançam mais que a maioria das configurações, merecem permissão própria em vez de reaproveitar `settings:write`
- [x] **Webhooks Enterprise** (`lib/webhooks/delivery.ts`, `services/webhook-endpoint.service.ts`): nomes de evento público desacoplados dos internos (`domain/api-v1/webhook-events.ts`), Circuit Breaker por endpoint (não por empresa), replay manual usando a mesma função de entrega do worker automático — ver ADR-037
- [x] **SDK oficial `@nfc-os/sdk`**: cliente HTTP tipado, erros tipados (`NFCOSApiError`), autopaginação (`async function*`), um recurso por área da API, açúcar `campaigns.activate()`/`.pause()` — completo mas não publicado no npm (ver ADR-038); alias `@nfc-os/sdk` adicionado ao `tsconfig.json` (mesmo padrão de `packages/ui`)
- [x] **Dashboard de Desenvolvedor** (`/dashboard/developers`, restrito a `developers:manage`): abas Chaves de API (criar/revogar, exibição única do valor completo), Webhooks (criar/editar/desativar/excluir, ver entregas, reenviar), Logs (chamadas reais com latência/status via `ApiRequestLog`), Documentação rápida (snippets copiáveis cURL/JS/TS/Node)
- [x] **Playground público** (`/developers`, com link no `SiteHeader`): quickstart, referência completa de recursos, Explorador de API ao vivo chamando `/api/v1/**` de verdade contra a Bella Vista com uma chave de demonstração somente-leitura semeada (`prisma/seed.ts`, `domain/api-v1/demo-key.ts`)
- [x] Achado e corrigido antes da entrega: `Branch`/`Zone` nunca tiveram capacidade de rename (nem no dashboard interno) — a paridade de CRUD da API pública tornou essa lacuna óbvia; adicionado `updateBranch`/`updateZone` (serviço + repositório + rota interna PATCH + rota v1 PATCH), com `lib/validations/{branch,zone}.ts` extraídos para serem compartilhados entre as duas superfícies em vez de duplicar o Zod inline que já existia
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo os ~30 novos arquivos de rota v1/dashboard/playground e o pacote `packages/sdk`
- [x] Verificação interativa via harness temporário (`middleware.ts`/`ClerkProvider` bypassados, restaurados depois) — confirmou o Playground renderizando por completo, o Explorador de API fazendo uma chamada real (401 sem header, 401 com chave malformada, 500 honesto com envelope de erro correto contra o Postgres inalcançável deste sandbox — sempre com `X-Request-Id` presente), provando toda a cadeia auth→rate-limit→erro funcionando de ponta a ponta mesmo sem banco real; `/dashboard/developers` não verificável neste sandbox (exige Clerk + Postgres reais, mesma limitação de todo `/dashboard/*` desde a Fase 1, não uma regressão desta fase)
- [x] `RELATORIO_FASE_9.md` entregue, incluindo Architect Review, Product Review, Demo First Review, Revenue Review, Reliability Review e as novas **Platform First Review** e **WOW Factor Review**

## Fase 10 — White Label Enterprise ✅

- [x] **Regras Permanentes — Enterprise Brand Review, Franchise First Review, Luxury UX** instituídas (as duas primeiras como sétimo/oitavo processos obrigatórios, ao lado de Architect/Product/Demo First/Revenue/Reliability/Platform First; a terceira como regra permanente de engenharia) — ver `ROADMAP.md`
- [x] **Schema:** `Company` ganhou `secondaryColor`, `domainVerifiedAt`, `domainVerificationToken`, `loginHeadline`, `loginBackgroundUrl` (além de `faviconUrl`/`domain`, preparados na Fase 9 — ver ADR-039)
- [x] **Domínio/cor** (`domain/white-label/`): `color.ts` (matemática de cor pura — `buildBrandColorSet`, `ensureScannableDark`, `readableTextColor`, sem biblioteca nova), `host.ts` (`classifyHost` — raiz/subdomínio/customizado), `types.ts` (`BrandConfig`, `companyToBrandConfig`), `print-templates.ts` (5 formatos com dimensões físicas reais)
- [x] **`DomainResolver`** (`lib/white-label/resolve-brand.ts`): resolve `Host` → `BrandConfig`, cacheado (`cachedOrLoad`, TTL 120s), nunca resolve domínio customizado não verificado; `lib/white-label/dns.ts` (verificação real via DNS-over-HTTPS/Cloudflare)
- [x] **`company.service.ts` estendido:** `claimDomain`/`removeDomain`/`verifyDomainOwnership`, invalidação de cache de marca por host junto com a invalidação já existente do Resolution Engine
- [x] **Rotas internas:** `PATCH /api/company` aceita os novos campos; `POST/DELETE /api/company/domain`, `POST /api/company/domain/verify`, `POST /api/company/branding/preview` (QR de rascunho sem salvar)
- [x] **`BrandProvider`** (`components/white-label/brand-provider.tsx`): único ponto de verdade em runtime, usado no layout do dashboard e nas telas de login — sem reescrever `AppSidebar` (ver ADR-040)
- [x] **Login com marca própria:** `BrandedAuthScreen` + `clerkAppearanceFor` (`components/white-label/branded-auth-screen.tsx`) — moldura visual + `appearance.variables.colorPrimary` do Clerk, autenticação nunca duplicada (ver ADR-041)
- [x] **Assets Inteligentes:** `/api/brand/icon` (favicon + Apple Touch Icon, com distintivo de inicial quando não há favicon próprio), `/api/brand/og` (Open Graph/Twitter Card), `/api/brand/manifest` (Web App Manifest, preparação PWA) — todos via `ImageResponse`/`next/og`, resolvidos por Host a cada requisição; `favicon.ico` estático removido (arquivado em `.archive/`) — ver ADR-043
- [x] **QR Code White Label:** `lib/qrcode.ts` ganhou `generateBrandedQrPngDataUrl`/`generateBrandedQrSvg`, sempre passando pela checagem `ensureScannableDark` antes de aceitar a cor da marca
- [x] **Impressão Profissional (bônus):** `domain/white-label/print-templates.ts` (5 formatos: adesivo, cartão PVC CR80, displex de mesa, cavalete A4, plaquinha), `services/export/print-asset-pdf.tsx` (template `@react-pdf/renderer`), `services/print-assets.service.ts`, `GET /api/cards/:id/print?template=X` — ver ADR-044
- [x] **Theme Studio** (`/dashboard/branding`, nova entrada no menu): `theme-studio-view.tsx` (rascunho em estado local, preview instantâneo sem salvar), `domain-panel.tsx` (reivindicar/verificar/remover domínio com instruções de TXT), `brand-preview-panels.tsx` (mockups de dashboard/login/QR/6 cenas físicas), `print-assets-panel.tsx` (downloads reais dos 5 PDFs)
- [x] **`/dashboard/settings` simplificado:** `logoUrl`/`primaryColor` e o campo de domínio permanentemente desabilitado ("em breve") removidos de lá — agora vivem só no Theme Studio, com um link direto; nunca duas telas editando o mesmo campo (ver ADR-040)
- [x] **Brand Motion System (bônus):** `.brand-glow`/`.brand-hover`/`.brand-ripple` em `globals.css` (usando `var(--brand-primary, var(--brand))` — cor da marca com fallback pro padrão do produto), `components/white-label/brand-ripple.tsx`, aplicados no Theme Studio
- [x] **Segurança:** primeiro CSP do produto (via `clerkMiddleware`, `report-only`) e HSTS real (`next.config.ts`) — nunca existiu nenhum dos dois antes desta fase, achado registrado sob Zero Dívida Silenciosa; ver ADR-045
- [x] **Preparação (não implementação) para as Fases 11/12/13** — ver ADR-046; Fase 13 (AI Copilot & Marketplace OS) registrada em `ROADMAP.md` como fase futura
- [x] Achado e corrigido antes da entrega, um erro real de build (não uma suposição): a primeira versão do `DomainResolver` importava o Prisma de dentro de `middleware.ts`; o Edge Runtime deste Next.js não sustenta APIs de Node (`UnhandledSchemeError` em `node:crypto`/`fs`/etc.) — corrigido movendo a resolução para dentro das páginas de login/cadastro (Server Components, runtime Node), que leem o próprio `Host` diretamente. Ver ADR-042
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo as novas rotas `next/og`/`ImageResponse`, o template `@react-pdf/renderer` novo, e o middleware com CSP
- [x] Verificação interativa via harness temporário (`middleware.ts`/`ClerkProvider` bypassados, restaurados depois) — ver `RELATORIO_FASE_10.md` para o que foi confirmado ao vivo e o que ficou fora do alcance deste sandbox
- [x] `RELATORIO_FASE_10.md` entregue, incluindo as oito reviews obrigatórias e as três regras permanentes de engenharia

## Fase 11 — Smart Campaign Playbooks ✅

- [x] **Regras Permanentes — Autonomy Review, Explainability First, One-Tap Execution** instituídas (a primeira como nono processo obrigatório; as outras duas como regras permanentes de engenharia) — ver `ROADMAP.md`
- [x] **Schema:** `Playbook` (catálogo global), `PlaybookRecommendation`, `PlaybookExecution`, `AutoPilotSetting`, mais `PlaybookTriggerType`/`PlaybookActionType`/`PlaybookCategory`/`RecommendationStatus`/`PlaybookExecutionStatus`/`AutoPilotLevel` e 4 novos `AuditAction` — ver ADR-048/ADR-049
- [x] **Domínio puro** (`domain/playbooks/`): `types.ts`, `confidence-engine.ts` (score 0-1 auditável), `impact.ts` (reaproveita `domain/analytics/roi.ts`), `triggers.ts` (7 avaliadores puros, um por `PlaybookTriggerType`), `present.ts` (apresentação compartilhada dashboard/Command Center)
- [x] **Os 5 motores pedidos:** `services/playbook-engine.service.ts` (avaliação + dedup anti-conflito), `services/recommendation-engine.service.ts` (ciclo de vida da recomendação), `services/execution-engine.service.ts` (preview/execução/undo/agendamento), `services/automation-engine.service.ts` (AutoPilot), `domain/playbooks/confidence-engine.ts`
- [x] **Event Bus estendido:** 3 eventos novos (`RecomendacaoGerada`/`PlaybookExecutado`/`PlaybookDesfeito`); nova fila `"playbooks"` (Queue Engine) inscrita SÓ em eventos de comportamento real do cliente (`RedirecionamentoResolvido`/`FeedbackRecebido`/`AvaliacaoPublicada`), nunca nos eventos que a própria execução produz — proteção estrutural contra loop de automação (ver ADR-050)
- [x] **Varredura periódica:** `GET /api/playbooks/evaluate` (Cron a cada 15min, `CRON_SECRET`, mesmo padrão de `/api/queues/process`) — cobre gatilhos de ausência de evento (zona silenciosa, mesa VIP parada)
- [x] **RBAC:** nova permissão `automation:manage` (OWNER/ADMIN apenas, para ligar o AutoPilot) — aplicar/ignorar reaproveita `campaign:assign`, mesmo raio de efeito de atribuir uma campanha manualmente
- [x] **Rotas internas:** `GET /api/playbooks`, `GET /api/playbooks/:id`, `GET /api/playbooks/:id/preview`, `POST /api/playbooks/:id/apply` (agora/depois/repetir), `POST /api/playbooks/:id/ignore`, `POST /api/playbooks/executions/:id/{undo,pause,resume,cancel}`, `GET/PATCH /api/automation/settings`
- [x] **Recommendation Center** (`/dashboard/playbooks`, nova entrada no menu): `RecommendationCard` novo em `packages/ui`, `PlaybookApplyDialog` (Ghost Mode + Preview Inteligente + Scheduler), `ExplainabilityPanel` (dados/comparação/confiança fatorada/regra disparada), `AutoPilotSelector` (4 níveis explicados)
- [x] **Executive Copilot:** coluna "Próximas melhores ações" no `/dev/ceo/command-center`, com a única rota de escrita que essa área já teve (`/api/dev/demo/playbooks/:id/apply`, gate tríplice de segurança) — exceção estreita e documentada a ADR-027 (ver ADR-051)
- [x] **Heatmap Preditivo:** `use-heatmap-layer.ts` compara janela atual vs. anterior (mesma rota `/api/heatmap`, sem endpoint novo), tendência "aquecendo"/"esfriando" por mesa, animação `.heatmap-breathe` (respeitando `prefers-reduced-motion`)
- [x] **Demo Premium:** seed da Bella Vista enviesado (conversão da Varanda maior às 18h-22h; Mesa VIP 1 sem toques há 4+ dias) para os dois playbooks mais narrativos dispararem de verdade, não fabricados
- [x] **Biblioteca de 7 Playbooks semeada:** Happy Hour Boost, Google Review Recovery, Instagram Momentum, VIP Table Recovery, Silent Zone Rescue, Lunch Rush Optimization, Weekend Accelerator — todos declarativos (`prisma/seed.ts`)
- [x] Achado e corrigido durante a própria construção (Zero Dívida Silenciosa/Architect Review): risco de loop infinito de automação identificado antes da entrega e fechado estruturalmente (fila de avaliação nunca escuta eventos que a própria execução produz) — ver ADR-050
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo as ~20 novas rotas e o novo componente `RecommendationCard` em `packages/ui`
- [x] Verificação interativa via harness temporário (`middleware.ts`/`ClerkProvider` bypassados, restaurados depois) — confirmou `/api/playbooks/evaluate` recusando sem `CRON_SECRET` (sem precisar de banco), e `/api/playbooks`/`/dashboard/playbooks`/`/dev/ceo/command-center` falhando na MESMA camada já conhecida (Clerk/Postgres indisponíveis neste sandbox), nunca uma classe nova de erro
- [x] `RELATORIO_FASE_11.md` entregue, incluindo as nove reviews obrigatórias e as cinco regras permanentes de engenharia

## Fase 12 — Developer Command Center v2 + Demo OS ✅

- [x] **Regras Permanentes — Demo OS Review, Self-Healing Development, Zero Fake Demo** instituídas (a primeira como nono processo obrigatório; as outras duas como regras permanentes de engenharia) — ver `ROADMAP.md`
- [x] **Dev Runtime** (`src/lib/dev-runtime/{config,auth}.ts`): `DEV_RUNTIME=1` faz `getAuthContext()` resolver para um `User` real semeado sem chamar o Clerk; `middleware.ts` (passthrough) e `layout.tsx` (sem `<ClerkProvider>`) reagem permanentemente — nunca mais editados à mão. Verificado ao vivo: `/dashboard` parou de lançar o erro do Clerk e passou a falhar exatamente no limite genuíno do sandbox (Postgres), stack trace confirmando `getDevRuntimeAuthContext → prisma.user.findFirst`. Ver ADR-052
- [x] **Scenario Engine** (`services/scenario-engine.service.ts`, `domain/demo/types.ts`): 7 cenários reais (Happy Hour, Restaurante Lotado, Avaliações Disparando, Zona Silenciosa, Franquia, Falha de Redis, AutoPilot Trabalhando) — cada um cria `RedirectLog`/`Visit`/`RatingEvent` reais e chama serviços reais (Playbook Engine Fase 11, Chaos Mode Fase 8, AutoPilot Fase 11). Ver ADR-053
- [x] **Demo OS** (`/demo`, público, `force-dynamic`): narrativa de 9 momentos (`components/demo/demo-os-view.tsx`), reaproveitando `TableMapView`, Heatmap Preditivo (Fase 11) e `BrandProvider` (Fase 10) por completo; rotas públicas `/api/demo/{heatmap,live/stream,live/history,table-map/*,kpis,playbooks,brands,scenarios}` (equivalentes de `/api/dev/demo/*`, mas nunca bloqueadas por `NODE_ENV`)
- [x] **Demo Timeline** (`components/demo/demo-timeline.tsx`): `usePlayback` (Fase 6) estendido com velocidade (1x/2x/5x) e salto para "momentos importantes", mantendo 100% de compatibilidade com o uso existente no Mapa de Mesas
- [x] **White Label Live Switch**: 2 empresas fictícias novas semeadas (Sushi House, Nova Steakhouse) + `listWhiteLabelDemoBrands()`, troca instantânea via `BrandProvider` sem reload
- [x] **Investor Mode** (`/demo/investor`): mesma `DemoOSView` com `autoPlay`, avançando sozinha pelos 9 momentos
- [x] **Dev Command Center v2** (`/dev/command-center`, bloqueado em produção): `services/command-center.service.ts` compõe `getReliabilitySnapshot()` (Fase 8) com webhooks/playbooks-AutoPilot/cenário atual — nunca recalcula. Abas Cockpit/State Inspector/Network Inspector/Visual Event Flow. Ver ADR-055
- [x] **Event Explorer** (`/dev/command-center/events`): `listRecentEventLogs`/`getEventWithCorrelation` (novo, em `replay.service.ts`) — payload/correlação/duração real/replay (reaproveitando `POST /api/dev/replay`, estendido com filtro por `id`)
- [x] **State Inspector** (`services/state-inspector.service.ts`): status/campanha/histórico/playbooks ativos/previsão/ROI por mesa, tudo composto de funções de serviço já existentes
- [x] **Network Inspector**: `ApiRequestLog` (Fase 9) exposto numa tela nova, com cache hit-rate (Resolution/Analytics Engine)
- [x] **Visual Event Flow** (`components/dev/visual-event-flow.tsx`): partículas reais (nunca decorativas) viajando por NFC→Resolution Engine→Event Bus→Analytics→Playbooks→Command Center, uma por evento genuíno de `EventLog`
- [x] **Nenhum banco embarcado/em memória construído** — decisão de escopo deliberada e documentada. Ver ADR-054
- [x] Achado e corrigido durante a própria construção: `/demo` tentava pré-renderizar estaticamente no build (congelaria os dados no momento do build) — corrigido com `export const dynamic = "force-dynamic"` em `/demo` e `/demo/investor`
- [x] Achado e corrigido sob Zero Dívida Silenciosa: a lista de tipos de evento do Event Replay (`/dev/ceo/reliability`) estava desatualizada desde a Fase 11 (faltavam os 3 eventos de Playbook) — corrigida
- [x] Quality gates (`tsc`, `eslint`, `prisma validate`, `npm run build`) — todos limpos, incluindo dois builds completos de produção (o primeiro pegou o bug real de pré-renderização estática do `/demo`)
- [x] Verificação interativa via o NOVO Dev Runtime (`DEV_RUNTIME=1` no `.env.local` temporário, `middleware.ts`/`layout.tsx` NUNCA editados) — confirmou `/api/demo/scenarios` funcionando sem banco, e `/demo`/`/dev/command-center` falhando na MESMA camada já conhecida (Postgres indisponível neste sandbox), nunca uma classe nova de erro
- [x] `RELATORIO_FASE_12.md` entregue, incluindo as nove reviews obrigatórias e as sete regras permanentes de engenharia

## Backlog (não iniciado)

- Fase 9.5 — Marketplace & Integrações (registrada em `ROADMAP.md`, não projetada)
- Fase 13 — AI Copilot & Marketplace OS (registrada em `ROADMAP.md`, não projetada)

## Próxima tarefa

Aguardando aprovação para iniciar a **Fase 13 — AI Copilot & Marketplace OS**.
